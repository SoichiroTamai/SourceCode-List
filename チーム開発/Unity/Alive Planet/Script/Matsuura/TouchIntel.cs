using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TouchIntel : MonoBehaviour
{
    private GameObject gO;
    private TouchMonitorScript TMS;

    // Start is called before the first frame update
    void Start()
    {
        //gO = GameObject.Find("TouchMonitorManage");
        //TMS = gO.GetComponent<TouchMonitorScript>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    public void Set()
    {
        return;
    }
}
